/*
 * UTF-8 详细注释说明：备用 IMU 解算接口声明。
 *
 * 不建议在科目一调试中同时混用 IMU.c 和 IMU_2.c 的输出，避免航向角来源不一致。
 */

/*
 * 主函数/科目一调用链：
 * 1. 当前科目一主链路优先使用 IMU.c 中的 IMU963RA 接口，本模块不是默认自动驾驶入口。
 * 2. 如果以后切回旧 ICM/备用姿态融合，需要在初始化和中断读取处把调用切到 Init_Gyro_Offset()/Get_Angles_ICM()。
 * 3. 本模块输出 euler_angle，作用等价于给 guandao 的 update_state() 提供航向角。
 * 4. 调试姿态漂移时可以对比本模块和 IMU963RA 的 yaw 结果，但不要两个模块同时写同一航向变量。
 */


/*
 * IMU_2.h
 *
 *  Created on: 2025年2月18日
 *      Author: ORRN
 */

#ifndef CODE_IMU_2_H_
#define CODE_IMU_2_H_


#define DELTA_T     0.0051f     // 定义计算周期,5ms
#define alpha           0.3f    // 定义一阶低通滤波器的alpha值,用于平滑加速度数据


// 存储陀螺仪的零点偏移数据
/**
 * 结构体说明：用于集中保存本模块的一组状态量/参数，字段通常会被初始化函数清零，并在周期函数中持续更新。
 * 所属模块：备用 IMU 姿态解算模块，保留旧算法供对照或回退。
 * 字段/取值：结构体成员或枚举项旁边保留行内注释；调试时优先关注索引、目标值、反馈值和输出量。
 * 科目一关系：这些状态会沿着“菜单选择 -> 主循环模式 -> 中断周期执行器输出”的链路被读取或更新。
 * 使用边界：不要在未初始化前直接使用，也不要在多个控制模式之间混用同一份状态而不清零。
 */
typedef struct {
    float x_data;
    float y_data;
    float z_data;
} Imu_gyro_param_t;

// 存储陀螺仪和加速度计的数据
/**
 * 结构体说明：用于集中保存本模块的一组状态量/参数，字段通常会被初始化函数清零，并在周期函数中持续更新。
 * 所属模块：备用 IMU 姿态解算模块，保留旧算法供对照或回退。
 * 字段/取值：结构体成员或枚举项旁边保留行内注释；调试时优先关注索引、目标值、反馈值和输出量。
 * 科目一关系：这些状态会沿着“菜单选择 -> 主循环模式 -> 中断周期执行器输出”的链路被读取或更新。
 * 使用边界：不要在未初始化前直接使用，也不要在多个控制模式之间混用同一份状态而不清零。
 */
typedef struct {
    float gyro_x;
    float gyro_y;
    float gyro_z;
    float acc_x;
    float acc_y;
    float acc_z;
} Imu_data_param_t;

// 存储四元数参数,四元数用于表示旋转
/**
 * 结构体说明：用于集中保存本模块的一组状态量/参数，字段通常会被初始化函数清零，并在周期函数中持续更新。
 * 所属模块：备用 IMU 姿态解算模块，保留旧算法供对照或回退。
 * 字段/取值：结构体成员或枚举项旁边保留行内注释；调试时优先关注索引、目标值、反馈值和输出量。
 * 科目一关系：这些状态会沿着“菜单选择 -> 主循环模式 -> 中断周期执行器输出”的链路被读取或更新。
 * 使用边界：不要在未初始化前直接使用，也不要在多个控制模式之间混用同一份状态而不清零。
 */
typedef struct {
    float q0;
    float q1;
    float q2;
    float q3;
} quater_param_t;

// 存储欧拉角参数,欧拉角用于表示三维空间中的旋转
/**
 * 结构体说明：用于集中保存本模块的一组状态量/参数，字段通常会被初始化函数清零，并在周期函数中持续更新。
 * 所属模块：备用 IMU 姿态解算模块，保留旧算法供对照或回退。
 * 字段/取值：结构体成员或枚举项旁边保留行内注释；调试时优先关注索引、目标值、反馈值和输出量。
 * 科目一关系：这些状态会沿着“菜单选择 -> 主循环模式 -> 中断周期执行器输出”的链路被读取或更新。
 * 使用边界：不要在未初始化前直接使用，也不要在多个控制模式之间混用同一份状态而不清零。
 */
typedef struct {
        float pitch;
        float roll;
        float yaw;
} euler_param_t;
extern  euler_param_t    euler_angle ;
/**
 * 接口说明：Init_Gyro_Offset()。完成模块或硬件资源初始化，通常在系统启动阶段调用一次。
 * 所属模块：备用 IMU 姿态解算模块，保留旧算法供对照或回退。
 * 参数说明：
 * - 无：该函数不需要外部输入参数。
 * 返回值：无返回值；结果通过全局变量、结构体字段或硬件输出体现。
 * 科目一关系：如果该函数处在科目一链路中，通常由 core0_main() 主循环、CCU61_CH0/CH1 中断或 Menu_Contral() 间接触发。
 * 注意事项：调用前确认相关全局状态和硬件初始化已经完成，避免在中断和主循环中重复抢占同一硬件资源。
 */
void Init_Gyro_Offset(void);
/**
 * 接口说明：Get_Angles_ICM()。读取当前模块保存的状态量，主要用于屏幕显示和调试。
 * 所属模块：备用 IMU 姿态解算模块，保留旧算法供对照或回退。
 * 参数说明：
 * - 无：该函数不需要外部输入参数。
 * 返回值：无返回值；结果通过全局变量、结构体字段或硬件输出体现。
 * 科目一关系：如果该函数处在科目一链路中，通常由 core0_main() 主循环、CCU61_CH0/CH1 中断或 Menu_Contral() 间接触发。
 * 注意事项：调用前确认相关全局状态和硬件初始化已经完成，避免在中断和主循环中重复抢占同一硬件资源。
 */
void Get_Angles_ICM(void);
/**
 * 接口说明：IMU_init()。完成模块或硬件资源初始化，通常在系统启动阶段调用一次。
 * 所属模块：备用 IMU 姿态解算模块，保留旧算法供对照或回退。
 * 参数说明：
 * - 无：该函数不需要外部输入参数。
 * 返回值：无返回值；结果通过全局变量、结构体字段或硬件输出体现。
 * 科目一关系：如果该函数处在科目一链路中，通常由 core0_main() 主循环、CCU61_CH0/CH1 中断或 Menu_Contral() 间接触发。
 * 注意事项：调用前确认相关全局状态和硬件初始化已经完成，避免在中断和主循环中重复抢占同一硬件资源。
 */
void IMU_init(void);//IMU初始化

#endif /* CODE_IMU_2_H_ */
