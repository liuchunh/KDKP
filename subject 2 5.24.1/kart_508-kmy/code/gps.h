/*
 * UTF-8 详细注释说明：GPS 辅助路线结构体和接口。
 *
 * GPS_work 保存自动驾驶阶段正在使用的 GPS 辅助点。
 * GPS_SWITCH_DISTANCE 决定接近辅助点多少米时切换到下一 GPS 点。
 */

/*
 * 主函数/科目一调用链：
 * 1. Init_All() 中调用 GPS_Init() 初始化 GNSS 串口；UART3 接收中断调用 gnss_uart_callback() 收数据。
 * 2. CCU61_CH0 中断检测 gnss_flag 后调用 gnss_data_parse()，Main_Key_Flag 打开后继续调用 update_gpsinformation() 更新经纬度缓存。
 * 3. core0_main() 在 Flash_Main_Read() 后，如果 GPS_WORK_FLAG 打开，会调用 GPS_WorkMap_Copy(&INS) 复制 GPS 辅助点。
 * 4. 记录模式可通过 recode_gps() 保存 GPS 校验点，自动驾驶时 trace_gps()/swtich_gps() 用于辅助判断路线段或显示调试。
 */


/*
 * gps.h
 *
 *  Created on: 2026年4月23日
 *      Author: 18905
 */

#ifndef CODE_GPS_H_
#define CODE_GPS_H_

// 宏定义

#define GPS_WORK_NUM                            30
#define GPS_WORK_FLAG                            1
#define GPS_SWITCH_DISTANCE                  2.0f
//外部变量

/**
 * 结构体说明：用于集中保存本模块的一组状态量/参数，字段通常会被初始化函数清零，并在周期函数中持续更新。
 * 所属模块：GPS 辅助记录和显示模块，不是科目一后轮驱动主链路，但用于路线校验和定位调试。
 * 字段/取值：结构体成员或枚举项旁边保留行内注释；调试时优先关注索引、目标值、反馈值和输出量。
 * 科目一关系：这些状态会沿着“菜单选择 -> 主循环模式 -> 中断周期执行器输出”的链路被读取或更新。
 * 使用边界：不要在未初始化前直接使用，也不要在多个控制模式之间混用同一份状态而不清零。
 */
typedef struct
{
        double lat;
        double lon;
        float theta;
        int16 gps_cheak_flag;

}GPS_points;

/**
 * 结构体说明：用于集中保存本模块的一组状态量/参数，字段通常会被初始化函数清零，并在周期函数中持续更新。
 * 所属模块：GPS 辅助记录和显示模块，不是科目一后轮驱动主链路，但用于路线校验和定位调试。
 * 字段/取值：结构体成员或枚举项旁边保留行内注释；调试时优先关注索引、目标值、反馈值和输出量。
 * 科目一关系：这些状态会沿着“菜单选择 -> 主循环模式 -> 中断周期执行器输出”的链路被读取或更新。
 * 使用边界：不要在未初始化前直接使用，也不要在多个控制模式之间混用同一份状态而不清零。
 */
typedef struct
{
        GPS_points points[GPS_WORK_NUM];
        float angle_gps;
        float azimuth_gps;
        double distance_gps ;

        uint8 gps_update_flag;
        int16 work_gps_length;
        uint16 gps_current_point;

}GPS_work;

/**
 * 枚举说明：用于描述运行模式、阶段或状态，实际取值会被菜单、控制流程或调试显示读取。
 * 所属模块：GPS 辅助记录和显示模块，不是科目一后轮驱动主链路，但用于路线校验和定位调试。
 * 字段/取值：结构体成员或枚举项旁边保留行内注释；调试时优先关注索引、目标值、反馈值和输出量。
 * 科目一关系：这些状态会沿着“菜单选择 -> 主循环模式 -> 中断周期执行器输出”的链路被读取或更新。
 * 使用边界：不要在未初始化前直接使用，也不要在多个控制模式之间混用同一份状态而不清零。
 */
typedef enum{
    FIND,
    LOST,
}Lost_Point;
//函数声明
/**
 * 接口说明：gps_work_init()。完成模块或硬件资源初始化，通常在系统启动阶段调用一次。
 * 所属模块：GPS 辅助记录和显示模块，不是科目一后轮驱动主链路，但用于路线校验和定位调试。
 * 参数说明：
 * - 无：该函数不需要外部输入参数。
 * 返回值：无返回值；结果通过全局变量、结构体字段或硬件输出体现。
 * 科目一关系：如果该函数处在科目一链路中，通常由 core0_main() 主循环、CCU61_CH0/CH1 中断或 Menu_Contral() 间接触发。
 * 注意事项：调用前确认相关全局状态和硬件初始化已经完成，避免在中断和主循环中重复抢占同一硬件资源。
 */
void gps_work_init(void);
/**
 * 接口说明：angle_plan()。完成本模块中的一个独立步骤，具体行为由函数体内的状态变量和硬件调用决定。
 * 所属模块：GPS 辅助记录和显示模块，不是科目一后轮驱动主链路，但用于路线校验和定位调试。
 * 参数说明：
 * - angle：角度或航向相关参数，除特别说明外单位为度。
 * 返回值：无返回值；结果通过全局变量、结构体字段或硬件输出体现。
 * 科目一关系：如果该函数处在科目一链路中，通常由 core0_main() 主循环、CCU61_CH0/CH1 中断或 Menu_Contral() 间接触发。
 * 注意事项：调用前确认相关全局状态和硬件初始化已经完成，避免在中断和主循环中重复抢占同一硬件资源。
 */
void angle_plan(float * angle);
/**
 * 接口说明：recode_gps()。记录当前位置/路线点，用于后续自动追踪。
 * 所属模块：GPS 辅助记录和显示模块，不是科目一后轮驱动主链路，但用于路线校验和定位调试。
 * 参数说明：
 * - state：惯导路线/车辆状态结构体指针，保存当前位姿、路线点和追踪索引。
 * 返回值：无返回值；结果通过全局变量、结构体字段或硬件输出体现。
 * 科目一关系：如果该函数处在科目一链路中，通常由 core0_main() 主循环、CCU61_CH0/CH1 中断或 Menu_Contral() 间接触发。
 * 注意事项：调用前确认相关全局状态和硬件初始化已经完成，避免在中断和主循环中重复抢占同一硬件资源。
 */
void recode_gps(guandao_state * state);
/**
 * 接口说明：swtich_gps()。处理 GPS 辅助点、GPS 显示或 GPS 校验逻辑。
 * 所属模块：GPS 辅助记录和显示模块，不是科目一后轮驱动主链路，但用于路线校验和定位调试。
 * 参数说明：
 * - 无：该函数不需要外部输入参数。
 * 返回值：返回 uint8 类型结果，通常用于上层判断状态、显示调试值或继续参与控制计算。
 * 科目一关系：如果该函数处在科目一链路中，通常由 core0_main() 主循环、CCU61_CH0/CH1 中断或 Menu_Contral() 间接触发。
 * 注意事项：调用前确认相关全局状态和硬件初始化已经完成，避免在中断和主循环中重复抢占同一硬件资源。
 */
uint8 swtich_gps(void);
/**
 * 接口说明：trace_gps()。执行路线追踪或科目阶段逻辑，输出目标速度和转向角。
 * 所属模块：GPS 辅助记录和显示模块，不是科目一后轮驱动主链路，但用于路线校验和定位调试。
 * 参数说明：
 * - e：惯导路线/车辆状态结构体指针，保存当前位姿、路线点和追踪索引。
 * 返回值：无返回值；结果通过全局变量、结构体字段或硬件输出体现。
 * 科目一关系：如果该函数处在科目一链路中，通常由 core0_main() 主循环、CCU61_CH0/CH1 中断或 Menu_Contral() 间接触发。
 * 注意事项：调用前确认相关全局状态和硬件初始化已经完成，避免在中断和主循环中重复抢占同一硬件资源。
 */
void trace_gps(guandao_state * e);
/**
 * 接口说明：update_gpsinformation()。周期更新内部状态，依赖中断或主循环按固定节拍调用。
 * 所属模块：GPS 辅助记录和显示模块，不是科目一后轮驱动主链路，但用于路线校验和定位调试。
 * 参数说明：
 * - 无：该函数不需要外部输入参数。
 * 返回值：无返回值；结果通过全局变量、结构体字段或硬件输出体现。
 * 科目一关系：如果该函数处在科目一链路中，通常由 core0_main() 主循环、CCU61_CH0/CH1 中断或 Menu_Contral() 间接触发。
 * 注意事项：调用前确认相关全局状态和硬件初始化已经完成，避免在中断和主循环中重复抢占同一硬件资源。
 */
void update_gpsinformation(void);
/**
 * 接口说明：double_to_int32()。完成本模块中的一个独立步骤，具体行为由函数体内的状态变量和硬件调用决定。
 * 所属模块：GPS 辅助记录和显示模块，不是科目一后轮驱动主链路，但用于路线校验和定位调试。
 * 参数说明：
 * - y：输入/输出参数，具体含义需要结合函数名和调用位置理解。
 * 返回值：返回 int32 类型结果，通常用于上层判断状态、显示调试值或继续参与控制计算。
 * 科目一关系：如果该函数处在科目一链路中，通常由 core0_main() 主循环、CCU61_CH0/CH1 中断或 Menu_Contral() 间接触发。
 * 注意事项：调用前确认相关全局状态和硬件初始化已经完成，避免在中断和主循环中重复抢占同一硬件资源。
 */
int32 double_to_int32(double y);
/**
 * 接口说明：int32_to_double()。完成本模块中的一个独立步骤，具体行为由函数体内的状态变量和硬件调用决定。
 * 所属模块：GPS 辅助记录和显示模块，不是科目一后轮驱动主链路，但用于路线校验和定位调试。
 * 参数说明：
 * - y：输入/输出参数，具体含义需要结合函数名和调用位置理解。
 * 返回值：返回 double 类型结果，通常用于上层判断状态、显示调试值或继续参与控制计算。
 * 科目一关系：如果该函数处在科目一链路中，通常由 core0_main() 主循环、CCU61_CH0/CH1 中断或 Menu_Contral() 间接触发。
 * 注意事项：调用前确认相关全局状态和硬件初始化已经完成，避免在中断和主循环中重复抢占同一硬件资源。
 */
double int32_to_double(int32 y);
/**
 * 接口说明：GPS_WorkMap_Copy()。处理 GPS 辅助点、GPS 显示或 GPS 校验逻辑。
 * 所属模块：GPS 辅助记录和显示模块，不是科目一后轮驱动主链路，但用于路线校验和定位调试。
 * 参数说明：
 * - e：惯导路线/车辆状态结构体指针，保存当前位姿、路线点和追踪索引。
 * 返回值：无返回值；结果通过全局变量、结构体字段或硬件输出体现。
 * 科目一关系：如果该函数处在科目一链路中，通常由 core0_main() 主循环、CCU61_CH0/CH1 中断或 Menu_Contral() 间接触发。
 * 注意事项：调用前确认相关全局状态和硬件初始化已经完成，避免在中断和主循环中重复抢占同一硬件资源。
 */
void GPS_WorkMap_Copy(guandao_state * e);
/**
 * 接口说明：GPS_Points_Show()。负责屏幕显示或菜单跳转，不直接改变底层硬件接线。
 * 所属模块：GPS 辅助记录和显示模块，不是科目一后轮驱动主链路，但用于路线校验和定位调试。
 * 参数说明：
 * - e：惯导路线/车辆状态结构体指针，保存当前位姿、路线点和追踪索引。
 * 返回值：无返回值；结果通过全局变量、结构体字段或硬件输出体现。
 * 科目一关系：如果该函数处在科目一链路中，通常由 core0_main() 主循环、CCU61_CH0/CH1 中断或 Menu_Contral() 间接触发。
 * 注意事项：调用前确认相关全局状态和硬件初始化已经完成，避免在中断和主循环中重复抢占同一硬件资源。
 */
void GPS_Points_Show(guandao_state * e);
/**
 * 接口说明：GPS_Work_SHOW()。负责屏幕显示或菜单跳转，不直接改变底层硬件接线。
 * 所属模块：GPS 辅助记录和显示模块，不是科目一后轮驱动主链路，但用于路线校验和定位调试。
 * 参数说明：
 * - 无：该函数不需要外部输入参数。
 * 返回值：无返回值；结果通过全局变量、结构体字段或硬件输出体现。
 * 科目一关系：如果该函数处在科目一链路中，通常由 core0_main() 主循环、CCU61_CH0/CH1 中断或 Menu_Contral() 间接触发。
 * 注意事项：调用前确认相关全局状态和硬件初始化已经完成，避免在中断和主循环中重复抢占同一硬件资源。
 */
void GPS_Work_SHOW(void);
#endif /* CODE_GPS_H_ */
