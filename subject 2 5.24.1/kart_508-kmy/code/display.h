/*
 * UTF-8 详细注释说明：菜单显示函数声明。
 *
 * 这些函数由 Menu_Contral() 串起来形成上电菜单。
 * 菜单执行结束后，主循环会根据 main_mode 进入记录、自动驾驶或测试。
 */

/*
 * 主函数/科目一调用链：
 * 1. core0_main() 在 Flash_Main_Read() 之后调用 Menu_Contral()，用户通过屏幕菜单选择 main_mode 和 conrtol_mode。
 * 2. 选择 Guandao_portion_1 时，菜单会设置 main_mode=Guandao_portion_1、conrtol_mode=GUANDAO，并调用 portion_1_reset() 清理上次追踪状态。
 * 3. 选择 Guandao_Recode_Mode 时，主循环进入 guandao_recode(&INS)，推车记录路线点。
 * 4. 选择 Rack_Test_Mode 时，主循环进入 Rack_Test_Run()，用于架上测试转向、后轮速度和直线保持。
 */


/*
 * display.h
 *
 *  Created on: 2025年11月20日
 *      Author: 18905
 */

#ifndef CODE_DISPLAY_H_
#define CODE_DISPLAY_H_

//外部变量
/**
 * 枚举说明：用于描述运行模式、阶段或状态，实际取值会被菜单、控制流程或调试显示读取。
 * 所属模块：IPS200 菜单模块，决定上电后进入记录、科目一自动驾驶、RackTest 等哪个主模式。
 * 字段/取值：结构体成员或枚举项旁边保留行内注释；调试时优先关注索引、目标值、反馈值和输出量。
 * 科目一关系：这些状态会沿着“菜单选择 -> 主循环模式 -> 中断周期执行器输出”的链路被读取或更新。
 * 使用边界：不要在未初始化前直接使用，也不要在多个控制模式之间混用同一份状态而不清零。
 */
typedef enum{
    Mode_IDLE,
    Guandao_Recode_Mode,
    Guandao_portion_1,
    Guandao_Voice,
    Guandao_Portion2_Recode,
    Guandao_portion_3,
    Rack_Test_Mode
}Mode_Choice;

extern Mode_Choice main_mode;

extern uint8 key_mode1;
extern uint8 key_mode2;
extern uint8 CarGo_Flag;
extern float *p;
//宏定义
#define IPS200_TYPE     (IPS200_TYPE_SPI)    //屏幕初始化宏定义
#define X(x)                    8*(x)
#define Y(y)                    16*(y)
//函数定义

/**
 * 接口说明：Display_Init()。完成模块或硬件资源初始化，通常在系统启动阶段调用一次。
 * 所属模块：IPS200 菜单模块，决定上电后进入记录、科目一自动驾驶、RackTest 等哪个主模式。
 * 参数说明：
 * - 无：该函数不需要外部输入参数。
 * 返回值：无返回值；结果通过全局变量、结构体字段或硬件输出体现。
 * 科目一关系：如果该函数处在科目一链路中，通常由 core0_main() 主循环、CCU61_CH0/CH1 中断或 Menu_Contral() 间接触发。
 * 注意事项：调用前确认相关全局状态和硬件初始化已经完成，避免在中断和主循环中重复抢占同一硬件资源。
 */
void Display_Init(void);
/**
 * 接口说明：Key_Get()。读取当前模块保存的状态量，主要用于屏幕显示和调试。
 * 所属模块：IPS200 菜单模块，决定上电后进入记录、科目一自动驾驶、RackTest 等哪个主模式。
 * 参数说明：
 * - 无：该函数不需要外部输入参数。
 * 返回值：返回 uint8 类型结果，通常用于上层判断状态、显示调试值或继续参与控制计算。
 * 科目一关系：如果该函数处在科目一链路中，通常由 core0_main() 主循环、CCU61_CH0/CH1 中断或 Menu_Contral() 间接触发。
 * 注意事项：调用前确认相关全局状态和硬件初始化已经完成，避免在中断和主循环中重复抢占同一硬件资源。
 */
uint8 Key_Get(void);                                                    //界面切换按键扫描
/**
 * 接口说明：prompt()。负责屏幕显示或菜单跳转，不直接改变底层硬件接线。
 * 所属模块：IPS200 菜单模块，决定上电后进入记录、科目一自动驾驶、RackTest 等哪个主模式。
 * 参数说明：
 * - 无：该函数不需要外部输入参数。
 * 返回值：无返回值；结果通过全局变量、结构体字段或硬件输出体现。
 * 科目一关系：如果该函数处在科目一链路中，通常由 core0_main() 主循环、CCU61_CH0/CH1 中断或 Menu_Contral() 间接触发。
 * 注意事项：调用前确认相关全局状态和硬件初始化已经完成，避免在中断和主循环中重复抢占同一硬件资源。
 */
void prompt(void);                                                     //提示信息巡回
/**
 * 接口说明：Menu_Contral()。负责屏幕显示或菜单跳转，不直接改变底层硬件接线。
 * 所属模块：IPS200 菜单模块，决定上电后进入记录、科目一自动驾驶、RackTest 等哪个主模式。
 * 参数说明：
 * - 无：该函数不需要外部输入参数。
 * 返回值：无返回值；结果通过全局变量、结构体字段或硬件输出体现。
 * 科目一关系：如果该函数处在科目一链路中，通常由 core0_main() 主循环、CCU61_CH0/CH1 中断或 Menu_Contral() 间接触发。
 * 注意事项：调用前确认相关全局状态和硬件初始化已经完成，避免在中断和主循环中重复抢占同一硬件资源。
 */
void Menu_Contral(void);                                            //主循环控制
/**
 * 接口说明：Menu_key_Operation_int16()。负责屏幕显示或菜单跳转，不直接改变底层硬件接线。
 * 所属模块：IPS200 菜单模块，决定上电后进入记录、科目一自动驾驶、RackTest 等哪个主模式。
 * 参数说明：
 * - param_t：输入/输出参数，具体含义需要结合函数名和调用位置理解。
 * 返回值：返回 int16 类型结果，通常用于上层判断状态、显示调试值或继续参与控制计算。
 * 科目一关系：如果该函数处在科目一链路中，通常由 core0_main() 主循环、CCU61_CH0/CH1 中断或 Menu_Contral() 间接触发。
 * 注意事项：调用前确认相关全局状态和硬件初始化已经完成，避免在中断和主循环中重复抢占同一硬件资源。
 */
int16 Menu_key_Operation_int16(int16 *param_t );//参数设置模式 通过指针直接访问变量地址 函数数据类型与参数类型一一对应
/**
 * 接口说明：Menu_key_Operation_float()。负责屏幕显示或菜单跳转，不直接改变底层硬件接线。
 * 所属模块：IPS200 菜单模块，决定上电后进入记录、科目一自动驾驶、RackTest 等哪个主模式。
 * 参数说明：
 * - param_t：输入/输出参数，具体含义需要结合函数名和调用位置理解。
 * 返回值：返回 float 类型结果，通常用于上层判断状态、显示调试值或继续参与控制计算。
 * 科目一关系：如果该函数处在科目一链路中，通常由 core0_main() 主循环、CCU61_CH0/CH1 中断或 Menu_Contral() 间接触发。
 * 注意事项：调用前确认相关全局状态和硬件初始化已经完成，避免在中断和主循环中重复抢占同一硬件资源。
 */
float Menu_key_Operation_float(float *param_t );//参数设置模式 通过指针直接访问变量地址 函数数据类型与参数类型一一对应
/**
 * 接口说明：Menu_Main()。负责屏幕显示或菜单跳转，不直接改变底层硬件接线。
 * 所属模块：IPS200 菜单模块，决定上电后进入记录、科目一自动驾驶、RackTest 等哪个主模式。
 * 参数说明：
 * - 无：该函数不需要外部输入参数。
 * 返回值：无返回值；结果通过全局变量、结构体字段或硬件输出体现。
 * 科目一关系：如果该函数处在科目一链路中，通常由 core0_main() 主循环、CCU61_CH0/CH1 中断或 Menu_Contral() 间接触发。
 * 注意事项：调用前确认相关全局状态和硬件初始化已经完成，避免在中断和主循环中重复抢占同一硬件资源。
 */
void Menu_Main(void);
/**
 * 接口说明：Menu_1()。负责屏幕显示或菜单跳转，不直接改变底层硬件接线。
 * 所属模块：IPS200 菜单模块，决定上电后进入记录、科目一自动驾驶、RackTest 等哪个主模式。
 * 参数说明：
 * - 无：该函数不需要外部输入参数。
 * 返回值：无返回值；结果通过全局变量、结构体字段或硬件输出体现。
 * 科目一关系：如果该函数处在科目一链路中，通常由 core0_main() 主循环、CCU61_CH0/CH1 中断或 Menu_Contral() 间接触发。
 * 注意事项：调用前确认相关全局状态和硬件初始化已经完成，避免在中断和主循环中重复抢占同一硬件资源。
 */
void Menu_1(void);
/**
 * 接口说明：Menu_Parameter()。负责屏幕显示或菜单跳转，不直接改变底层硬件接线。
 * 所属模块：IPS200 菜单模块，决定上电后进入记录、科目一自动驾驶、RackTest 等哪个主模式。
 * 参数说明：
 * - 无：该函数不需要外部输入参数。
 * 返回值：无返回值；结果通过全局变量、结构体字段或硬件输出体现。
 * 科目一关系：如果该函数处在科目一链路中，通常由 core0_main() 主循环、CCU61_CH0/CH1 中断或 Menu_Contral() 间接触发。
 * 注意事项：调用前确认相关全局状态和硬件初始化已经完成，避免在中断和主循环中重复抢占同一硬件资源。
 */
void Menu_Parameter(void);
/**
 * 接口说明：Menu_2Value()。负责屏幕显示或菜单跳转，不直接改变底层硬件接线。
 * 所属模块：IPS200 菜单模块，决定上电后进入记录、科目一自动驾驶、RackTest 等哪个主模式。
 * 参数说明：
 * - 无：该函数不需要外部输入参数。
 * 返回值：无返回值；结果通过全局变量、结构体字段或硬件输出体现。
 * 科目一关系：如果该函数处在科目一链路中，通常由 core0_main() 主循环、CCU61_CH0/CH1 中断或 Menu_Contral() 间接触发。
 * 注意事项：调用前确认相关全局状态和硬件初始化已经完成，避免在中断和主循环中重复抢占同一硬件资源。
 */
void Menu_2Value(void);
/**
 * 接口说明：Menu_Control_Value()。负责屏幕显示或菜单跳转，不直接改变底层硬件接线。
 * 所属模块：IPS200 菜单模块，决定上电后进入记录、科目一自动驾驶、RackTest 等哪个主模式。
 * 参数说明：
 * - 无：该函数不需要外部输入参数。
 * 返回值：无返回值；结果通过全局变量、结构体字段或硬件输出体现。
 * 科目一关系：如果该函数处在科目一链路中，通常由 core0_main() 主循环、CCU61_CH0/CH1 中断或 Menu_Contral() 间接触发。
 * 注意事项：调用前确认相关全局状态和硬件初始化已经完成，避免在中断和主循环中重复抢占同一硬件资源。
 */
void Menu_Control_Value(void);
/**
 * 接口说明：Menu_Mode_Choice()。负责屏幕显示或菜单跳转，不直接改变底层硬件接线。
 * 所属模块：IPS200 菜单模块，决定上电后进入记录、科目一自动驾驶、RackTest 等哪个主模式。
 * 参数说明：
 * - 无：该函数不需要外部输入参数。
 * 返回值：无返回值；结果通过全局变量、结构体字段或硬件输出体现。
 * 科目一关系：如果该函数处在科目一链路中，通常由 core0_main() 主循环、CCU61_CH0/CH1 中断或 Menu_Contral() 间接触发。
 * 注意事项：调用前确认相关全局状态和硬件初始化已经完成，避免在中断和主循环中重复抢占同一硬件资源。
 */
void Menu_Mode_Choice(void);
/**
 * 接口说明：Menu_Recode_Points()。负责屏幕显示或菜单跳转，不直接改变底层硬件接线。
 * 所属模块：IPS200 菜单模块，决定上电后进入记录、科目一自动驾驶、RackTest 等哪个主模式。
 * 参数说明：
 * - 无：该函数不需要外部输入参数。
 * 返回值：无返回值；结果通过全局变量、结构体字段或硬件输出体现。
 * 科目一关系：如果该函数处在科目一链路中，通常由 core0_main() 主循环、CCU61_CH0/CH1 中断或 Menu_Contral() 间接触发。
 * 注意事项：调用前确认相关全局状态和硬件初始化已经完成，避免在中断和主循环中重复抢占同一硬件资源。
 */
void Menu_Recode_Points(void);
/**
 * 接口说明：GPS_prompt()。负责屏幕显示或菜单跳转，不直接改变底层硬件接线。
 * 所属模块：IPS200 菜单模块，决定上电后进入记录、科目一自动驾驶、RackTest 等哪个主模式。
 * 参数说明：
 * - 无：该函数不需要外部输入参数。
 * 返回值：无返回值；结果通过全局变量、结构体字段或硬件输出体现。
 * 科目一关系：如果该函数处在科目一链路中，通常由 core0_main() 主循环、CCU61_CH0/CH1 中断或 Menu_Contral() 间接触发。
 * 注意事项：调用前确认相关全局状态和硬件初始化已经完成，避免在中断和主循环中重复抢占同一硬件资源。
 */
void GPS_prompt(void);
/**
 * 接口说明：Menu_Show_Route()。负责屏幕显示或菜单跳转，不直接改变底层硬件接线。
 * 所属模块：IPS200 菜单模块，决定上电后进入记录、科目一自动驾驶、RackTest 等哪个主模式。
 * 参数说明：
 * - 无：该函数不需要外部输入参数。
 * 返回值：无返回值；结果通过全局变量、结构体字段或硬件输出体现。
 * 科目一关系：如果该函数处在科目一链路中，通常由 core0_main() 主循环、CCU61_CH0/CH1 中断或 Menu_Contral() 间接触发。
 * 注意事项：调用前确认相关全局状态和硬件初始化已经完成，避免在中断和主循环中重复抢占同一硬件资源。
 */
void Menu_Show_Route(void);
/**
 * 接口说明：Show_Route()。负责屏幕显示或菜单跳转，不直接改变底层硬件接线。
 * 所属模块：IPS200 菜单模块，决定上电后进入记录、科目一自动驾驶、RackTest 等哪个主模式。
 * 参数说明：
 * - 无：该函数不需要外部输入参数。
 * 返回值：无返回值；结果通过全局变量、结构体字段或硬件输出体现。
 * 科目一关系：如果该函数处在科目一链路中，通常由 core0_main() 主循环、CCU61_CH0/CH1 中断或 Menu_Contral() 间接触发。
 * 注意事项：调用前确认相关全局状态和硬件初始化已经完成，避免在中断和主循环中重复抢占同一硬件资源。
 */
void Show_Route(void);
/**
 * 接口说明：Menu_PID_P()。负责屏幕显示或菜单跳转，不直接改变底层硬件接线。
 * 所属模块：IPS200 菜单模块，决定上电后进入记录、科目一自动驾驶、RackTest 等哪个主模式。
 * 参数说明：
 * - 无：该函数不需要外部输入参数。
 * 返回值：无返回值；结果通过全局变量、结构体字段或硬件输出体现。
 * 科目一关系：如果该函数处在科目一链路中，通常由 core0_main() 主循环、CCU61_CH0/CH1 中断或 Menu_Contral() 间接触发。
 * 注意事项：调用前确认相关全局状态和硬件初始化已经完成，避免在中断和主循环中重复抢占同一硬件资源。
 */
void Menu_PID_P(void);
/**
 * 接口说明：Menu_Control_P()。负责屏幕显示或菜单跳转，不直接改变底层硬件接线。
 * 所属模块：IPS200 菜单模块，决定上电后进入记录、科目一自动驾驶、RackTest 等哪个主模式。
 * 参数说明：
 * - 无：该函数不需要外部输入参数。
 * 返回值：无返回值；结果通过全局变量、结构体字段或硬件输出体现。
 * 科目一关系：如果该函数处在科目一链路中，通常由 core0_main() 主循环、CCU61_CH0/CH1 中断或 Menu_Contral() 间接触发。
 * 注意事项：调用前确认相关全局状态和硬件初始化已经完成，避免在中断和主循环中重复抢占同一硬件资源。
 */
void Menu_Control_P(void);
#endif /* CODE_DISPLAY_H_ */
