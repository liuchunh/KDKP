/*
 * UTF-8 详细注释说明：整车外设引脚、宏和底层驱动接口。
 *
 * 这里是查接线最重要的文件之一：
 * - KEY1~KEY4、SWITCH1/2。
 * - 后轮编码器 TIM2 P33_7/P33_6。
 * - 后轮电机 PWM/GPIO。
 * - 舵机 PWM 和蜂鸣器引脚。
 *
 * 改接线时应先改这里的宏，再确认对应初始化函数是否也使用同一宏。
 */

/*
 * 主函数/科目一调用链：
 * 1. core0_main() 首先调用 Init_All()，Init_All() 集中初始化屏幕、按键、蜂鸣器、编码器、电机、IMU、GPS 和惯导状态。
 * 2. CCU61_CH1 中断周期调用 Key_Scan()、IMU_GetValues()；CCU61_CH0 中断周期调用转向控制、GPS 解析和后轮编码器采样。
 * 3. 科目一记录模式通过 Encoder_Get(&guandao_ecd) 获取后轮里程；当前左右反馈共用左后轮编码器。
 * 4. Rack_Test_Run() 是架上调试入口，用来分别验证前轮转向、后轮速度和 IMU 直线保持，避免一上来就跑完整科目一。
 */


/*
 * peripheral.h
 *
 *  Created on: 2025年11月20日
 *      Author: 18905
 */

#ifndef CODE_PERIPHERAL_H_
#define CODE_PERIPHERAL_H_


//外部变量
extern uint8 TIM_FLAG1;
extern uint8 TIM_FLAG2;
extern uint8 TIM_FLAG3 ;
extern uint8 key1_flag ;
extern uint8 key2_flag ;
extern uint8 key3_flag ;
extern uint8 key4_flag ;
extern uint8 key_val;
extern uint8 key_value;

extern int16 encoder_l ;
extern int16 encoder_r ;
extern int motor_pwm_l;
extern int motor_pwm_r;
extern uint8 rack_test_stage;
extern int16 rack_test_speed_target;
extern int32 rack_test_steer_target;
extern float rack_straight_target_yaw;
extern float rack_straight_yaw_error;
extern float rack_straight_steer_target;

/**
 * 结构体说明：用于集中保存本模块的一组状态量/参数，字段通常会被初始化函数清零，并在周期函数中持续更新。
 * 所属模块：整车外设底层模块，主函数初始化和中断周期任务都依赖这里。
 * 字段/取值：结构体成员或枚举项旁边保留行内注释；调试时优先关注索引、目标值、反馈值和输出量。
 * 科目一关系：这些状态会沿着“菜单选择 -> 主循环模式 -> 中断周期执行器输出”的链路被读取或更新。
 * 使用边界：不要在未初始化前直接使用，也不要在多个控制模式之间混用同一份状态而不清零。
 */
typedef struct{
        int16 left_counter;
        int16 right_counter;
        int32 delta_l;
        int32 delta_r;
        int16 last_ecdcount_l;
        int16 last_ecdcount_r;
}Encoder_t;

extern Encoder_t Speed_ecd;
extern Encoder_t guandao_ecd;
extern Encoder_t Steer_ecd;

//宏定义
#define r_ecdcounter()    encoder_get_count(ENCODER_QUADDEC)
#define l_ecdcounter()    encoder_get_count(ENCODER_QUADDEC)
#define BUZZER_PIN  (P33_10)      //蜂鸣器

#define KEY1                    (P20_6)    //按键引脚配置   //P20_6  //P11_3
#define KEY2                    (P20_7)                               //P20_7 //P11_2
#define KEY3                    (P11_2)                                //P11_3 //P20_7
#define KEY4                    (P11_3)                               //P11_2 //P20_6
#define SWITCH2                 (P33_12)
#define SWITCH1                 (P33_11)

#define SERVO_MOTOR_PWM             (ATOM1_CH1_P33_9)
#define SERVO_MOTOR_FREQ            (50)
#define SERVO_MOTOR_DUTY(x)         ((float)PWM_DUTY_MAX/(1000.0/(float)SERVO_MOTOR_FREQ)*(0.27+(float)(x)/90.0))
#define SERVO_MOTOR_MID             (80)                                        //中值80
#define SERVO_MOTOR_LMAX            (65)                                        //左打死60
#define SERVO_MOTOR_RMAX            (95)                                       //右打死100

#define ENCODER_QUADDEC                 (TIM2_ENCODER)
#define ENCODER_QUADDEC_A               (TIM2_ENCODER_CH1_P33_7)
#define ENCODER_QUADDEC_B               (TIM2_ENCODER_CH2_P33_6)

#define PWM_L              (ATOM0_CH3_P21_5)
#define PWM_R              (ATOM0_CH1_P21_3)
#define MOTOR_GPIO_L              (P21_4)
#define MOTOR_GPIO_R              (P21_2)
#define MOTER_MAX       (7000)
#define MOTER_MIN       (-7000)
#define S_MOTER_MAX       (5000)
#define S_MOTER_MIN       (-5000)
//函数

/**
 * 接口说明：Init_All()。完成模块或硬件资源初始化，通常在系统启动阶段调用一次。
 * 所属模块：整车外设底层模块，主函数初始化和中断周期任务都依赖这里。
 * 参数说明：
 * - 无：该函数不需要外部输入参数。
 * 返回值：无返回值；结果通过全局变量、结构体字段或硬件输出体现。
 * 科目一关系：如果该函数处在科目一链路中，通常由 core0_main() 主循环、CCU61_CH0/CH1 中断或 Menu_Contral() 间接触发。
 * 注意事项：调用前确认相关全局状态和硬件初始化已经完成，避免在中断和主循环中重复抢占同一硬件资源。
 */
void Init_All(void);
/**
 * 接口说明：Key_Init()。完成模块或硬件资源初始化，通常在系统启动阶段调用一次。
 * 所属模块：整车外设底层模块，主函数初始化和中断周期任务都依赖这里。
 * 参数说明：
 * - 无：该函数不需要外部输入参数。
 * 返回值：无返回值；结果通过全局变量、结构体字段或硬件输出体现。
 * 科目一关系：如果该函数处在科目一链路中，通常由 core0_main() 主循环、CCU61_CH0/CH1 中断或 Menu_Contral() 间接触发。
 * 注意事项：调用前确认相关全局状态和硬件初始化已经完成，避免在中断和主循环中重复抢占同一硬件资源。
 */
void Key_Init(void);   //按键
/**
 * 接口说明：Key_Scan()。处理按键输入、按键标志或按键触发的参数修改。
 * 所属模块：整车外设底层模块，主函数初始化和中断周期任务都依赖这里。
 * 参数说明：
 * - 无：该函数不需要外部输入参数。
 * 返回值：无返回值；结果通过全局变量、结构体字段或硬件输出体现。
 * 科目一关系：如果该函数处在科目一链路中，通常由 core0_main() 主循环、CCU61_CH0/CH1 中断或 Menu_Contral() 间接触发。
 * 注意事项：调用前确认相关全局状态和硬件初始化已经完成，避免在中断和主循环中重复抢占同一硬件资源。
 */
void Key_Scan(void);
/**
 * 接口说明：Buzzer_Init()。完成模块或硬件资源初始化，通常在系统启动阶段调用一次。
 * 所属模块：整车外设底层模块，主函数初始化和中断周期任务都依赖这里。
 * 参数说明：
 * - 无：该函数不需要外部输入参数。
 * 返回值：无返回值；结果通过全局变量、结构体字段或硬件输出体现。
 * 科目一关系：如果该函数处在科目一链路中，通常由 core0_main() 主循环、CCU61_CH0/CH1 中断或 Menu_Contral() 间接触发。
 * 注意事项：调用前确认相关全局状态和硬件初始化已经完成，避免在中断和主循环中重复抢占同一硬件资源。
 */
void Buzzer_Init(void);
/**
 * 接口说明：Buzzer_check()。驱动蜂鸣器发声，用于保存成功、模式切换等提示。
 * 所属模块：整车外设底层模块，主函数初始化和中断周期任务都依赖这里。
 * 参数说明：
 * - time2：时间周期或采样间隔，速度计算时会参与单位换算。
 * 返回值：无返回值；结果通过全局变量、结构体字段或硬件输出体现。
 * 科目一关系：如果该函数处在科目一链路中，通常由 core0_main() 主循环、CCU61_CH0/CH1 中断或 Menu_Contral() 间接触发。
 * 注意事项：调用前确认相关全局状态和硬件初始化已经完成，避免在中断和主循环中重复抢占同一硬件资源。
 */
void Buzzer_check(int time2); //蜂鸣器的自检函数
/**
 * 接口说明：Steer_init()。完成模块或硬件资源初始化，通常在系统启动阶段调用一次。
 * 所属模块：整车外设底层模块，主函数初始化和中断周期任务都依赖这里。
 * 参数说明：
 * - 无：该函数不需要外部输入参数。
 * 返回值：无返回值；结果通过全局变量、结构体字段或硬件输出体现。
 * 科目一关系：如果该函数处在科目一链路中，通常由 core0_main() 主循环、CCU61_CH0/CH1 中断或 Menu_Contral() 间接触发。
 * 注意事项：调用前确认相关全局状态和硬件初始化已经完成，避免在中断和主循环中重复抢占同一硬件资源。
 */
void Steer_init(void);               //舵机初始化
/**
 * 接口说明：Steer_set()。写入上层给定的目标值或执行器输出，并在函数内部做必要限幅。
 * 所属模块：整车外设底层模块，主函数初始化和中断周期任务都依赖这里。
 * 参数说明：
 * - angle：角度或航向相关参数，除特别说明外单位为度。
 * 返回值：无返回值；结果通过全局变量、结构体字段或硬件输出体现。
 * 科目一关系：如果该函数处在科目一链路中，通常由 core0_main() 主循环、CCU61_CH0/CH1 中断或 Menu_Contral() 间接触发。
 * 注意事项：调用前确认相关全局状态和硬件初始化已经完成，避免在中断和主循环中重复抢占同一硬件资源。
 */
void Steer_set(int angle);        //舵机驱动
/**
 * 接口说明：Steer_text()。驱动电机或转向执行器，调用前需要确认方向和限幅。
 * 所属模块：整车外设底层模块，主函数初始化和中断周期任务都依赖这里。
 * 参数说明：
 * - 无：该函数不需要外部输入参数。
 * 返回值：无返回值；结果通过全局变量、结构体字段或硬件输出体现。
 * 科目一关系：如果该函数处在科目一链路中，通常由 core0_main() 主循环、CCU61_CH0/CH1 中断或 Menu_Contral() 间接触发。
 * 注意事项：调用前确认相关全局状态和硬件初始化已经完成，避免在中断和主循环中重复抢占同一硬件资源。
 */
void Steer_text(void);             //舵机测试
/**
 * 接口说明：Encoder_Init()。完成模块或硬件资源初始化，通常在系统启动阶段调用一次。
 * 所属模块：整车外设底层模块，主函数初始化和中断周期任务都依赖这里。
 * 参数说明：
 * - 无：该函数不需要外部输入参数。
 * 返回值：无返回值；结果通过全局变量、结构体字段或硬件输出体现。
 * 科目一关系：如果该函数处在科目一链路中，通常由 core0_main() 主循环、CCU61_CH0/CH1 中断或 Menu_Contral() 间接触发。
 * 注意事项：调用前确认相关全局状态和硬件初始化已经完成，避免在中断和主循环中重复抢占同一硬件资源。
 */
void Encoder_Init(void);             //编码器初始化
/**
 * 接口说明：Encoder_Get()。读取当前模块保存的状态量，主要用于屏幕显示和调试。
 * 所属模块：整车外设底层模块，主函数初始化和中断周期任务都依赖这里。
 * 参数说明：
 * - count：编码器相关输入或计数值，用于速度、里程或角度换算。
 * 返回值：无返回值；结果通过全局变量、结构体字段或硬件输出体现。
 * 科目一关系：如果该函数处在科目一链路中，通常由 core0_main() 主循环、CCU61_CH0/CH1 中断或 Menu_Contral() 间接触发。
 * 注意事项：调用前确认相关全局状态和硬件初始化已经完成，避免在中断和主循环中重复抢占同一硬件资源。
 */
void Encoder_Get(Encoder_t *count);             //编码器读取
/**
 * 接口说明：Motor_init()。完成模块或硬件资源初始化，通常在系统启动阶段调用一次。
 * 所属模块：整车外设底层模块，主函数初始化和中断周期任务都依赖这里。
 * 参数说明：
 * - 无：该函数不需要外部输入参数。
 * 返回值：无返回值；结果通过全局变量、结构体字段或硬件输出体现。
 * 科目一关系：如果该函数处在科目一链路中，通常由 core0_main() 主循环、CCU61_CH0/CH1 中断或 Menu_Contral() 间接触发。
 * 注意事项：调用前确认相关全局状态和硬件初始化已经完成，避免在中断和主循环中重复抢占同一硬件资源。
 */
void Motor_init(void);              //电机初始化
/**
 * 接口说明：Moter_Set()。写入上层给定的目标值或执行器输出，并在函数内部做必要限幅。
 * 所属模块：整车外设底层模块，主函数初始化和中断周期任务都依赖这里。
 * 参数说明：
 * - moter_l：PWM 或电机输出值，正负号通常表示方向。
 * - moter_r：PWM 或电机输出值，正负号通常表示方向。
 * 返回值：无返回值；结果通过全局变量、结构体字段或硬件输出体现。
 * 科目一关系：如果该函数处在科目一链路中，通常由 core0_main() 主循环、CCU61_CH0/CH1 中断或 Menu_Contral() 间接触发。
 * 注意事项：调用前确认相关全局状态和硬件初始化已经完成，避免在中断和主循环中重复抢占同一硬件资源。
 */
void Moter_Set(int moter_l , int moter_r);   //电机驱动
/**
 * 接口说明：LimitMax()。对输入变量进行限幅，防止控制量超过安全范围。
 * 所属模块：整车外设底层模块，主函数初始化和中断周期任务都依赖这里。
 * 参数说明：
 * - input：输入/输出参数，具体含义需要结合函数名和调用位置理解。
 * - max：限幅边界值，用于保护控制输出或参数范围。
 * 返回值：返回 int 类型结果，通常用于上层判断状态、显示调试值或继续参与控制计算。
 * 科目一关系：如果该函数处在科目一链路中，通常由 core0_main() 主循环、CCU61_CH0/CH1 中断或 Menu_Contral() 间接触发。
 * 注意事项：调用前确认相关全局状态和硬件初始化已经完成，避免在中断和主循环中重复抢占同一硬件资源。
 */
int LimitMax(int input, int max);    //函数内限幅形参
/**
 * 接口说明：Control()。完成本模块中的一个独立步骤，具体行为由函数体内的状态变量和硬件调用决定。
 * 所属模块：整车外设底层模块，主函数初始化和中断周期任务都依赖这里。
 * 参数说明：
 * - 无：该函数不需要外部输入参数。
 * 返回值：无返回值；结果通过全局变量、结构体字段或硬件输出体现。
 * 科目一关系：如果该函数处在科目一链路中，通常由 core0_main() 主循环、CCU61_CH0/CH1 中断或 Menu_Contral() 间接触发。
 * 注意事项：调用前确认相关全局状态和硬件初始化已经完成，避免在中断和主循环中重复抢占同一硬件资源。
 */
void Control(void);                          //电机&舵机按键调试
/**
 * 接口说明：Rack_Test_Run()。完成本模块中的一个独立步骤，具体行为由函数体内的状态变量和硬件调用决定。
 * 所属模块：整车外设底层模块，主函数初始化和中断周期任务都依赖这里。
 * 参数说明：
 * - 无：该函数不需要外部输入参数。
 * 返回值：无返回值；结果通过全局变量、结构体字段或硬件输出体现。
 * 科目一关系：如果该函数处在科目一链路中，通常由 core0_main() 主循环、CCU61_CH0/CH1 中断或 Menu_Contral() 间接触发。
 * 注意事项：调用前确认相关全局状态和硬件初始化已经完成，避免在中断和主循环中重复抢占同一硬件资源。
 */
void Rack_Test_Run(void);
/**
 * 接口说明：Rack_Straight_Reset()。清零内部状态和控制输出，用于重新进入测试/自动驾驶前恢复初始状态。
 * 所属模块：整车外设底层模块，主函数初始化和中断周期任务都依赖这里。
 * 参数说明：
 * - 无：该函数不需要外部输入参数。
 * 返回值：无返回值；结果通过全局变量、结构体字段或硬件输出体现。
 * 科目一关系：如果该函数处在科目一链路中，通常由 core0_main() 主循环、CCU61_CH0/CH1 中断或 Menu_Contral() 间接触发。
 * 注意事项：调用前确认相关全局状态和硬件初始化已经完成，避免在中断和主循环中重复抢占同一硬件资源。
 */
void Rack_Straight_Reset(void);
/**
 * 接口说明：Rack_Straight_Update()。周期更新内部状态，依赖中断或主循环按固定节拍调用。
 * 所属模块：整车外设底层模块，主函数初始化和中断周期任务都依赖这里。
 * 参数说明：
 * - 无：该函数不需要外部输入参数。
 * 返回值：无返回值；结果通过全局变量、结构体字段或硬件输出体现。
 * 科目一关系：如果该函数处在科目一链路中，通常由 core0_main() 主循环、CCU61_CH0/CH1 中断或 Menu_Contral() 间接触发。
 * 注意事项：调用前确认相关全局状态和硬件初始化已经完成，避免在中断和主循环中重复抢占同一硬件资源。
 */
void Rack_Straight_Update(void);
/**
 * 接口说明：GPS_Init()。完成模块或硬件资源初始化，通常在系统启动阶段调用一次。
 * 所属模块：整车外设底层模块，主函数初始化和中断周期任务都依赖这里。
 * 参数说明：
 * - 无：该函数不需要外部输入参数。
 * 返回值：无返回值；结果通过全局变量、结构体字段或硬件输出体现。
 * 科目一关系：如果该函数处在科目一链路中，通常由 core0_main() 主循环、CCU61_CH0/CH1 中断或 Menu_Contral() 间接触发。
 * 注意事项：调用前确认相关全局状态和硬件初始化已经完成，避免在中断和主循环中重复抢占同一硬件资源。
 */
void GPS_Init(void);
/**
 * 接口说明：Encoder_count_init()。完成模块或硬件资源初始化，通常在系统启动阶段调用一次。
 * 所属模块：整车外设底层模块，主函数初始化和中断周期任务都依赖这里。
 * 参数说明：
 * - count：编码器相关输入或计数值，用于速度、里程或角度换算。
 * 返回值：无返回值；结果通过全局变量、结构体字段或硬件输出体现。
 * 科目一关系：如果该函数处在科目一链路中，通常由 core0_main() 主循环、CCU61_CH0/CH1 中断或 Menu_Contral() 间接触发。
 * 注意事项：调用前确认相关全局状态和硬件初始化已经完成，避免在中断和主循环中重复抢占同一硬件资源。
 */
void Encoder_count_init(Encoder_t *count);
/**
 * 接口说明：VeerMoter_Set()。写入上层给定的目标值或执行器输出，并在函数内部做必要限幅。
 * 所属模块：整车外设底层模块，主函数初始化和中断周期任务都依赖这里。
 * 参数说明：
 * - moter：PWM 或电机输出值，正负号通常表示方向。
 * 返回值：无返回值；结果通过全局变量、结构体字段或硬件输出体现。
 * 科目一关系：如果该函数处在科目一链路中，通常由 core0_main() 主循环、CCU61_CH0/CH1 中断或 Menu_Contral() 间接触发。
 * 注意事项：调用前确认相关全局状态和硬件初始化已经完成，避免在中断和主循环中重复抢占同一硬件资源。
 */
void VeerMoter_Set(int moter );
#endif /* CODE_PERIPHERAL_H_ */
