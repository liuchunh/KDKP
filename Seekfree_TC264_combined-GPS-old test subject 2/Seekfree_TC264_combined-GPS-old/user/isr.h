/*********************************************************************************************************************
 * 文件名称: isr.h
 * 功能描述: 中断服务程序头文件 - 科目1 自动驾驶
 *
 * 本文件声明了所有 ISR 函数和相关的外部句柄变量
 * isr.c 中的 ISR 实现通过 #include "isr.h" 引用本文件
 *
 * 注意:
 *   1. ISR 函数使用 IFX_INTERRUPT 宏定义, 不需要手动声明
 *   2. 本文件主要声明 ISR 中需要引用的外部变量和句柄
 *   3. uart_handle 等句柄由 Seekfree 库自动创建
 ********************************************************************************************************************/

#ifndef ISR_H_
#define ISR_H_

#include "zf_common_headfile.h"

/* =====================================================================
 *  外部变量声明 — ISR 中需要引用的全局变量
 * ===================================================================== */

/* ---- PIT 定时器标志 (定义在 cpu0_main.c 中) ---- */
extern volatile uint8 g_imu_tick;       /* IMU 采样定时标志 */
extern volatile uint8 g_debug_tick;     /* 调试输出定时标志 */

/* =====================================================================
 *  串口句柄声明 — 由 Seekfree 库创建, ISR 错误处理需要引用
 * ===================================================================== */

extern IfxAsclin_Asc uart0_handle;      /* UART0 句柄 (无线模块) */
extern IfxAsclin_Asc uart1_handle;      /* UART1 句柄 (预留) */
extern IfxAsclin_Asc uart2_handle;      /* UART2 句柄 (调试串口) */
extern IfxAsclin_Asc uart3_handle;      /* UART3 句柄 (无线模块) */

#endif /* ISR_H_ */
